# static-site-generator
guided project by bootdotdev for a static HTML site generator

## run tests
`./tests.sh`

## check test coverage
this requires the python3 coverage module to be installed.

`./tests.sh coverage`
